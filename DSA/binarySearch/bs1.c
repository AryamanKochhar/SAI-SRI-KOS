#include <stdio.h>
//&arr[] is used to find the address of the whole array
//arr[] is used to find the address of the first element


int main(){
    int c,first,last,middle,n,search,array[100];
    printf("ENter the number of elements \n ");
    scanf("%d",&n);
    printf("enter %d integers \n ",n );
    for(int i=0;i<n;i++)
    {
        scanf("%d",&array[c])

    }
    return 0;
}